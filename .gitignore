# Secrets — the real key never gets committed
.env

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ChromaDB persisted vectors (rebuilt by ingest.py)
chroma_db/

# Redirected HOME for the ONNX embedder cache on Render (built into the artifact)
.render-home/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
